<!-- placeholder -->
<!--
  This file has not been filled in yet. Before doing any real literature
  work in this repo, work through the questions below with the user and
  replace this whole file with real content — then delete this comment
  block and the placeholder marker above.

  Keep the finished version SHORT (a page or so). This gets read in full
  at the start of every session; it is not the place for the full project
  brief — that's what project_documents/ is for. This is the distilled
  "what counts as in-scope" reference that makes gap-flagging possible.
-->

# Study scope

## What is this study about?

<One or two paragraphs: the topic, in plain terms. What question(s) is
this literature study ultimately in service of?>

## In scope

<Bullet list of sub-topics, methods, domains, or angles that clearly
belong in this study. Be specific enough that a borderline paper can
actually be checked against this list.>

## Out of scope

<Bullet list of adjacent things that are deliberately excluded — this is
often more useful than the in-scope list for catching borderline cases.
Include *why* something is excluded if it's not obvious (e.g. "X is a
mature, separate field with its own literature; this study assumes X as
a given input rather than studying it").>

## What "a gap" means here

<A gap is something inside scope that the current PDF collection doesn't
cover well. State anything that would help distinguish that from "just
not relevant" — e.g. specific sub-questions the study needs answered,
or a rule like "if it doesn't touch [X], it's out of scope even if it
mentions [topic keyword] in passing".>

## Working language

<What language are the source documents in, and what language does this
study write in? State both, even when they're the same — this is settled
implicitly and early or not at all, and a study where two people started
on different days ends up with a half-and-half catalog that nobody wants
to fix later.

Cover: the language(s) source documents arrive in; the language the
catalog, notes, and all analysis are written in; and what to do with a
quoted passage from a source in another language (translate it, keep the
original, or keep both). Any subject can have non-English sources, so
this is worth a line even if it looks obvious today.>

## Intended use / audience

<Who is this study for, and what will its findings actually be used for?
Affects how much weight to put on e.g. peer-reviewed vs. preprint sources,
how deep the synthesis needs to go, whether practical/implementation
detail matters or only high-level findings do.>

## Status

<Living document — update whenever the study's understanding of its own
boundaries changes. Note the last real update here so staleness is
visible at a glance, e.g. "Last reviewed: 2026-09-04".>
